{
    "name": "Default",
    "description": "The factory look. Every section is declared empty on purpose: applying this clears the stored overrides so colors, header, footer, homepage and page settings all fall back to settings.json. Nothing here can drift from the theme defaults because nothing here duplicates them.",
    "settings": {
        "theme": {},
        "header": {},
        "footer": {},
        "homepage": {},
        "homepage_config": {},
        "tool_page": {}
    }
}
