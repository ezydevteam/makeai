# Node.js & NPM
node_modules/
npm-debug.log
yarn-error.log
yarn-clean.log
pnpm-debug.log
.lerna

# Laravel & PHP
/vendor/
.env
.env.backup
.env.laragon
.env.production
.env.testing
# Hand-made copies of an env file. Named per-host and per-experiment, so the list above
# can never keep up — and every one of them holds the same credentials .env does.
.env.*.bak
.env.bak
/bootstrap/cache/*
/public/hot
/public/storage

# Zero-config distribution structure: Root storage
/storage/
!/storage/.gitkeep
/storage/app/public/
!/storage/app/public/.gitkeep
/storage/framework/
!/storage/framework/.gitkeep

# PHP / IDE Helpers
_ide_helper.php
_ide_helper_models.php
.phpstorm.meta.php

# PHPUnit & Testing
.phpunit.result.cache
.phpunit.cache/

# IDEs & Editors
.idea/
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS Files
.DS_Store
Thumbs.db

# AI Agent Specific Files & Exclusions (Local Environment Only)
AGENT.md
CLAUDE.md
.agent-mem/
.agent/
.agents/
.cursor
.windsurfrules
.cursorrules
.clineRules
.gemini/
.cline/
.mcp-memory/


# Auto-generated agent rules (personalized per developer)
.brainsync/agent-rules.md

# Export files
storage/app/exports/

# Distribution build output
/dist/

# Playwright MCP browser console logs (tooling artefact, not project state)
/.playwright-mcp/
