# Taste (Continuously Learned by [CommandCode][cmd])

[cmd]: https://commandcode.ai/

# Architecture
- AI generation fields should use global settings (default provider + model, fallback provider + model) instead of hardcoded values. Confidence: 0.75

# admin-ui
- Use AppSelect and AppColorPicker components instead of native &lt;select&gt; and color text inputs in admin panel interfaces. Confidence: 0.65
